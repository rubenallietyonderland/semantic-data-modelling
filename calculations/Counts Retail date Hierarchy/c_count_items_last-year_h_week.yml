unique_name: c_count_items_last-year_h_week
object_type: metric_calc
label: Count Items -1 Year
expression: >-
  CASE WHEN ISEMPTY([Measures].[m_FCT_ATSCALE_SERVICES_ITEM_ID_count]) THEN 

  NULL 

  ELSE 

  (ParallelPeriod([Created DIM_DATES_QUERY_dimension].[Created
  DIM_DATES_QUERY_retail Hierarchy].[Created DIM_DATES_QUERY_retail_year], 1,
  [Created DIM_DATES_QUERY_dimension].[Created DIM_DATES_QUERY_retail
  Hierarchy].CurrentMember), [Measures].[m_FCT_ATSCALE_SERVICES_ITEM_ID_count]) 

  END
mdx_aggregate_function: COUNT
