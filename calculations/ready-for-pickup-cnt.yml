unique_name: ready-for-pickup-cnt
object_type: metric_calc
label: ready-for-pickup-cnt
expression: |-
  COUNT(
    FILTER(
      [FCT_WARRANTY_CASES].[CASE_ID].MEMBERS,
      ([FCT_WARRANTY_CASES].[LOGISTIC_STATUS].CURRENTMEMBER.NAME = "ready-for-pickup")
    )
  )
is_hidden: false
mdx_aggregate_function: COUNT
