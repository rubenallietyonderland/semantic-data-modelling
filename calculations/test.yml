unique_name: test
object_type: metric_calc
label: test
expression: |-
  CASE WHEN ISEMPTY([Measures].[m_orders_count]) THEN 

    NULL 
    
    ELSE

  (ParallelPeriod([date_test_dim].[date_test_dim Hierarchy].[year], 1,
    [date_test_dim].[date_test_dim Hierarchy].CurrentMember),
    [Measures].[m_orders_count])

    END
is_hidden: false
