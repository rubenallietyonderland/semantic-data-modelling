unique_name: c_count_orders_last-year
object_type: metric_calc
label: Count Orders Last Year
expression: >-
  CASE WHEN ISEMPTY([Measures].[m_FCT_ATSCALE_SERVICES_ORDER_ID_count]) THEN 

  NULL 

  ELSE (ParallelPeriod([DIM_DATES_dimension].[DIM_DATES_dimension
  Hierarchy].[DIM_DATES_dimension_year], 1,
  [DIM_DATES_dimension].[DIM_DATES_dimension Hierarchy].CurrentMember),
  [Measures].[m_FCT_ATSCALE_SERVICES_ORDER_ID_count]) END
is_hidden: false
mdx_aggregate_function: COUNT
